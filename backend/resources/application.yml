server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://db:5432/soundpulse
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
